/**
 * The main handler helper module for registering helpers.
 */

define(
  [
    'handlebars',
    'lib/handlebars/helper/FormatCurrencyHelper'
  ],
  function(HandleBars) {
    return HandleBars;
  }
);